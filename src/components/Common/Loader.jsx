import React from "react";
import { BubbleLoader } from "react-css-loaders";

/** loader component with default and general styling */
const Loader = () => (
  <BubbleLoader color="#1a2633" style={{ marginBottom: "-80px" }} />
);

export default Loader;
